# TODO for office session — 2026-05-13

> Read [HANDOFF-2026-05-12-evening.md](HANDOFF-2026-05-12-evening.md) first for context.

## Status snapshot at handoff close

| Metric | Value |
|---|---|
| HEAD on origin/main | `4a3ef9c` |
| Deploy gate state | GREEN — all 7 jobs |
| Demo synced | Yes (v3.7.5 + post-tag patches) |
| Open issues | 11 (1 intentional + 10 new pen-test items) |
| Open PRs | 0 |

## Priority order

### 1. 🔴 Triage new pen-test wave (#711-720) — start here

10 fresh pen-test items filed this morning. **Phantom-carry-over risk: same pen-test team that re-opened ~17 already-closed issues today.** Run `verifying-issue-before-pickup` on each before dispatching agents.

#### HIGH severity (4)

- **#711 [BUG][HIGH] Profile — Change Password accepts weak / short passwords (no length, required or complexity policy)**
  - Likely real. Check `backend/routes/auth.js` POST `/change-password` for validation. If absent, add: min 8 chars, mix of letter+digit, ≤72 chars (bcrypt limit). Effort: S (~1h).
- **#712 [BUG][HIGH] Privacy — saving negative retention value silently logs the user out (no validation)**
  - The "silently logs user out" is the load-bearing bit. Likely `backend/routes/gdpr.js` PUT `/retention-policies` 500s on negative integer + frontend treats 500 as auth-loss. Add server-side `if (retainDays < 0)` 400 + frontend doesn't logout on 400. Effort: S (~2h).
- **#713 [SECURITY][HIGH] Developer — Webhooks endpoint URL accepts `javascript:` and other non-http(s) schemes**
  - `backend/routes/developer.js` or `backend/routes/webhooks.js` POST handler. Add `new URL(url).protocol === 'http:' || 'https:'` validation. Effort: XS (~30 min). Same fix pattern as the SSRF guards in #545.
- **#714 [BUG][HIGH] Staff Directory — Edit user accepts empty Name and invalid email (server-side validation gap)**
  - `backend/routes/staff.js` PUT handler. Add `if (!name?.trim() || !isValidEmail(email))` guards. Effort: S (~1h). Check if `lib/validateInput.js` already has email validator.

#### MEDIUM severity (4)

- **#716 [BUG][MEDIUM] Channels / MSG91 — Sender ID labeled "(6 chars)" accepts any length with no validation**
  - Cosmetic + functional. Either drop the "(6 chars)" label OR enforce server-side. Likely `backend/routes/sms.js` PUT `/config` doesn't validate `senderId.length === 6`. Effort: XS.
- **#717 [BUG][MEDIUM] Revenue Goals — New goal save fails with "targetUserId is required" even when a staff member is selected**
  - Frontend likely sends `userId` instead of `targetUserId` (per CLAUDE.md's stripDangerous rule). Compare to v3.7.x revenue-goals fixes (commit `0ef1a71`). Effort: XS (~30 min).
- **#718 [UI][MEDIUM] Revenue Goals — New goal dialog: label column disappears after first failed save (broken grid layout)**
  - CSS grid bug. Likely a `display: contents` getting toggled off. Effort: S (~1h).
- **#719 [BUG][MEDIUM] Currencies — BASE column shows no currency selected even though header tile says "Default Base: USD ($)"**
  - State-sync bug between page header and list rows. Effort: S (~1-2h).

#### LOW severity (2)

- **#715 [UI][LOW] Organization Settings — Slug input appears editable but silently discards changes (no read-only styling, no error)**
  - Add `readOnly` attribute + show "slug is read-only after creation" hint. Effort: XS (~15 min).
- **#720 [BUG][LOW] Developer / API Credentials — Generate Key creates a key even when Key Name is empty (no required validation)**
  - Frontend + backend validation. Effort: XS (~30 min).

**Dispatch plan:** one focused agent on 4 HIGH items (bundled commit), one on 4 MEDIUM (bundled), one on 2 LOW (bundled). All ~6-8 hours dispatched runtime.

### 2. 🟡 Watch for PR #710 follow-up re-opens

The pen-test team re-opened ~17 issues today against pre-deploy demo state, before today's fixes had landed. They likely re-test on demo tomorrow. **If issues 670/672/673/674/675/678/679/681/682/684-689/692/693/694-707 re-open again:**

- First action: verify the symptom against current demo (`https://crm.globusdemos.com` running HEAD `4a3ef9c`)
- If the symptom IS gone → close with a curt citation comment ("HEAD `4a3ef9c` demo deploy GREEN; re-tested OK")
- If the symptom IS present → it's a real new variant (e.g. the fix landed but didn't cover this specific surface); file as a fresh sub-issue with the new reproducer

### 3. 🟢 Optional: tag v3.7.6

Since v3.7.5 there have been 12+ commits:
- `dbd8f9d` skills (2 NEW + 2 extensions)
- `ceaa429` PENDING doc refresh
- All-issues-sweep wave commits (`6cc8887`, `e4980d3`, `8bcd96f`, `822ab9c`)
- Wave B/C/D commits (`f85dc45`, `a30a40d`, `ab046d4`, `885645a`, `b4ea83b`, `1364fea`, `2a4e21e`, `feb0fcc`)
- `0a242b6` test-shape flip
- PR #710 + 4 rounds of fallout (`dc02453`, `6301249`, `62fb8d8`, `1940f28`, `4a3ef9c`)

Substantial surface area. Worth a tag for release-validation if you want a clean baseline before #711-720 lands. Use `bumping-version-docs` skill.

### 4. 🟢 Optional: backfill audit-log hash chain on demo

Per PENDING_USER_AND_OPERATOR.md §3 (#558 closed in v3.7.5), there's an optional follow-up:
```bash
node backend/scripts/backfill-audit-chain.js --dry-run --json
# Drop --dry-run after sanity-checking the count
```
Backfills any pre-#558 audit rows accumulated since v3.4.x. Not blocking; nice-to-have.

## NOT for office session

- **#457** Manual-only QA umbrella — intentional, stays open
- ~~**PENDING_USER_AND_OPERATOR.md §1 SendGrid Sender Identity**~~ ✅ **CLOSED 2026-05-13 morning** — Sumit verified `noreply@crm.globusdemos.com`; smoke-test green
- **PENDING_USER_AND_OPERATOR.md §6 / §7** — Callified webhook + AdsGPT SSO are external-team deliverables

## Standing rules to apply (don't re-derive)

Before any dispatch, the skills system handles:

- `.claude/skills/verifying-issue-before-pickup/` — 30-second grep per dispatched issue (phantom-carry-over has 6+ instances)
- `.claude/skills/dispatching-parallel-agent-wave/` — when overlapping files are touched, use patcher pattern
- `.claude/skills/batch-closing-issues-after-multi-fix-commit/` — verify-and-close loop after multi-trailer commits
- `.claude/skills/auditing-cross-cutting-spec-impact/` — response-shape grep checklist

The pattern for an external-author PR with stale base + fallout: **selective-merge** (`git checkout main -- <reverted-file>` on PR branch + push fixup) — referenced PR #566 `b78e484` as canonical. PR #710's stack today is the 2nd instance of this pattern.

## Pickup command for the next session

```bash
# Sync + verify state
git pull origin main
gh pr list --state open
gh issue list --state=open --label "HIGH,CRITICAL"

# Then read this doc + start at section 1
```

---

**Last updated:** 2026-05-12 evening (after PR #710 4-round fallout green + re-closure sweep of 17 phantom-re-opened issues).
