# Travel CRM — big-scope backlog (active build)

**Active from 2026-06-09.** Successor to the closed [TRAVEL_CODEABLE_BACKLOG.md](TRAVEL_CODEABLE_BACKLOG.md) (9/9 shipped). Targets the 4 multi-day big-scope features identified in [TRAVEL_CRM_PENDING_FEATURES.md](TRAVEL_CRM_PENDING_FEATURES.md).

> **Markers:** ⬜ TODO · 🟡 IN-PROGRESS · ✅ DONE · 🔵 BLOCKED

**Honest scope:** of the 4 big-scope features, ~25-36 eng-days are codeable today; the rest gate on product calls, creds, or counsel. Cron will skip 🔵 rows and dispatch ⬜ rows whose deps are ✅.

## Block 1 — Travel Security architecture

Per [PRD_TRAVEL_SECURITY_ARCHITECTURE.md](PRD_TRAVEL_SECURITY_ARCHITECTURE.md). 5 of 7 FRs are codeable today; FR-3.1 (auth migration) and FR-3.3 (opaque IDs) are blocked on architectural DDs.

| # | Slice | Files | Marker | Cost | Notes |
|---|---|---|---|---|---|
| **S1** | FR-3.2 — CSP nonces + remove `'unsafe-inline'` | `backend/middleware/security.js` (extend) + `backend/lib/cspNonce.js` (NEW) + `frontend/index.html` (template change for nonce injection) + tests | ✅ DONE 2026-06-09 — `02c71f7e` | ~3d | Replace `'unsafe-inline'` in script-src + style-src with per-request nonces. Backend mints nonce per request → injected into HTML via meta tag → vite-build-time CSS gets nonce attr. Closes #917. Backward-compat: report-only mode first via FR-3.7. Deploy-layer follow-up: Nginx (or Express static handler) must substitute `__CSP_NONCE__` placeholder in served index.html with the live `res.locals.cspNonce` per response and stamp the matching nonce onto every `<script>`/`<style>` tag. |
| **S2** | FR-3.4 — Tenant-scope audit across 102 routes + ESLint rule | `backend/eslint.config.js` (extend) + `e2e/tests/cross-tenant-coverage-audit.spec.js` (NEW) + audit doc | ✅ DONE 2026-06-09 — `63a1a0dd` | ~4d | ESLint rule warns when a Prisma call inside `routes/**/*.js` is missing `tenantId` in WHERE (inline `gbscrm/tenant-scope-finder-heuristic` plugin rule, warn-level, narrowed to `findMany` only — 69 callsites currently warn-flagged). Cron-callsite review separately under #919. E2E spec [`cross-tenant-coverage-audit.spec.js`](../e2e/tests/cross-tenant-coverage-audit.spec.js) probes 16 cases across Contact / Deal / Invoice / Quote / Patient / TravelItinerary / TravelQuote / TravelInvoice / TmcTrip / VisaApplication / TripParticipant / ApiKey / AuditLog + 3 cross-cutting (anon, nonexistent-id, ?tenantId fan-out). Audit doc at [docs/gaps/cross-tenant-coverage-audit.md](gaps/cross-tenant-coverage-audit.md). Closes #918 + (partial) #919. |
| **S3** | FR-3.5 — PII list-endpoint summary projections | `backend/lib/listProjection.js` (NEW) + 8-12 route extensions for list endpoints (contacts/leads/patients/trip participants/etc.) + tests | ✅ DONE 2026-06-09 — `b8ca16b9` | ~3d | List endpoints today return full row shape including PII. New helper auto-applies `select { id, name, ...summaryFields }` projection on list endpoints; detail endpoint returns full shape. Reduces PII exposure surface. Closes #920. Shipped helper `backend/lib/listProjection.js` (42 vitest cases) + 8 list endpoints across 7 route files (`travel_trips.js` ×2 / `travel_itineraries.js` / `travel_quotes.js` / `travel_invoices.js` / `travel_suppliers.js` / `travel_rfu_profiles.js` / `marketplace_leads.js`) wired through the helper; opt-in via `?fields=summary` (matches the 51 prior `?fields=summary` slices 1-51, kept opt-in direction to preserve back-compat for the 51 + every existing frontend consumer). Cross-cutting audit clean: zero existing frontend or e2e consumers pass `?fields=summary` for the touched routes. The PRD's "summary-as-default" direction is a follow-up cross-cutting break (S37 candidate). |
| **S4** | FR-3.6 — iframe-isolation + SRI build step | `frontend/vite.config.js` (extend with SRI plugin) + `backend/middleware/security.js` (CSP frame-ancestors) + tests | ✅ DONE 2026-06-09 — `6561bdcd` | ~3d | SRI hashes on vendor chunks (inline vite plugin computing `sha384` integrity attrs at `transformIndexHtml.post`); X-Frame-Options flipped SAMEORIGIN → DENY and CSP frame-ancestors flipped 'self' → 'none' as global defaults; `allowIframeEmbedding({ allowList })` factory shipped for per-route override (the embed widget at `/embed/lead-form.html` wire-in is a follow-up — server.js was outside slice file scope). Per-tenant `Tenant.embedAllowlistJson` allowlist read stubbed at `readTenantEmbedAllowlist()` returning null; schema add deferred to a follow-up slice (column doesn't exist yet). Cross-cutting CSP/XFO update touched 3 existing specs (security-headers, csp-stepup-api, auth-security-regression-api). Closes #921. |
| **S5** | FR-3.7 — CSP-report endpoint + cross-tenant interceptor | `backend/routes/security_reports.js` (NEW) + `backend/middleware/crossTenantInterceptor.js` (NEW) + tests | ✅ DONE 2026-06-09 — `ec3c25e5` | ~2d | POST `/api/security/csp-report` collects violation reports; saves to `SecurityIncident` model (additive). Cross-tenant interceptor is request-time defense: if route's tenant-where filter would fan out, intercept before DB. Closes #921. |
| **S6** | 🔵 BLOCKED — FR-3.1 auth migration (JWT → httpOnly cookie + csurf + AuthContext refactor + per-tenant flag) | `backend/middleware/auth.js` (replace) + `frontend/src/App.jsx` (AuthContext refactor) + many | 🔵 BLOCKED | ~5d | **Blocked on:** DD-5.1 (cookie shape decision — same-site=strict vs lax; refresh-token rotation policy), DD-5.5 (rollout cadence — flag-gated per tenant or big-bang). Critical-path before any production client. |
| **S7** | 🔵 BLOCKED — FR-3.3 sequential IDs → opaque IDs (`publicId` column on 14+ models + dual-route + backfill) | `backend/prisma/schema.prisma` (publicId on 14 models, requires bless marker — UNIQUE on backfilled column) + dual-route middleware + per-route Prisma extensions | 🔵 BLOCKED | ~6d | **Blocked on:** DD-5.2 (id-migration shape — nanoid vs UUIDv7 vs hashids; column name; index strategy). Once decided, runs as 14+ parallel sub-slices. |

## Block 2 — Itinerary visual editor

Per [PRD_TRAVEL_ITINERARY_UPGRADES.md](PRD_TRAVEL_ITINERARY_UPGRADES.md). Most sub-features are codeable; Mapbox + Gemini cred items have stub-mode fallbacks per PRD §Q-IT-1/Q-IT-2.

| # | Slice | Files | Marker | Cost | Notes |
|---|---|---|---|---|---|
| **S8** | Add `dayNumber` + `latitude` + `longitude` columns on ItineraryItem | `backend/prisma/schema.prisma` (additive nullable, no bless marker) + migration safety | ✅ DONE 2026-06-09 — `81099782` | ~½d | Pure schema add. Used by S9 (visual editor) + S11 (map preview). |
| **S9** | Day-by-day visual itinerary editor (drag-drop reorder + add/remove day groups) | `frontend/src/pages/travel/ItineraryDayEditor.jsx` (NEW) + `frontend/src/__tests__/ItineraryDayEditor.test.jsx` + App.jsx route + backend support for batch-update `dayNumber` | ⬜ TODO | ~5d | Per FR-3.3. Drag-drop across days, intra-day reorder. Uses the same touch-event pattern from C4 (Pipeline.jsx). Posts batch updates `PATCH /api/travel/itineraries/:id/items/reorder` with `[{itemId, dayNumber, sortOrder}]`. Depends on S8 (schema). |
| **S10** | Leaflet+OSM map provider integration + lightweight Geocoder | `frontend/src/components/MapPreview.jsx` (NEW) + `frontend/src/lib/geocoder.js` (NEW, OSM Nominatim wrapper with rate limiting + cache) | ✅ DONE 2026-06-10 — `<SHA>` | ~2d | Per FR-3.4. No API key needed — uses public OSM tile servers (with attribution). Component renders pins for ItineraryItems with lat/lng. Cached geocoder hits Nominatim with `User-Agent` header. Depends on S8 (lat/lng columns). **Shipped:** `MapPreview.jsx` (react-leaflet `MapContainer` + OSM `TileLayer` + per-day-coloured `Marker`+`Popup` palette + auto-fit bbox with degenerate single-pin fallback + manual `centerLat`/`centerLng`/`zoom` override + visible "© OpenStreetMap contributors" attribution overlay + fail-soft on draft items without lat/lng) + `geocoder.js` (500-entry LRU keyed by normalized query / 6-dp rounded coords + 1-req/sec single-chain rate limiter + canonical `User-Agent: GlobussoftCRM/1.0 (…)` header + fail-soft `null` returns on network / non-OK / empty result + `clearCache` test seam + `__test__` namespace for cache + cap inspection) + 22 `MapPreview.test.jsx` vitest + 16 `geocoder.test.js` vitest. Consumer wire-in (Itineraries.jsx / ItineraryDetail.jsx / ItineraryDayEditor.jsx) deferred to a follow-up gap slice. |
| **S11** | OpenTripMap POI seed import (~500 POIs across India + Saudi + Europe destinations) | `backend/scripts/seedOpenTripMapPois.js` (NEW) + `backend/prisma/seed-travel-pois.js` (NEW) + sample fixture | ⬜ TODO | ~1d | Per FR-3.5. Free API key (Yasin needs to sign up at opentripmap.io — ~5 min self-serve, NOT a "wait on Yasin" blocker; agent can use placeholder env var). Top 50 POIs per top-10 destinations. Idempotent upsert. |
| **S12** | Inline Add-POI modal with `pendingApproval` queue | `frontend/src/pages/travel/PoiPendingApprovalQueue.jsx` (NEW) + `backend/routes/travel_pois.js` (extend with approve/reject) + tests | ⬜ TODO | ~2d | Per FR-3.7. Reps suggest a new POI inline; lands in approval queue; ADMIN approves/rejects. Depends on S11 (POI model). |
| **S13** | Brand-kit-aware itinerary template defaults from `subBrandConfigJson` | `backend/routes/travel_itinerary_templates.js` (extend creation to apply brand-kit defaults) + tests | ✅ DONE 2026-06-09 — `1541a063` | ~1d | Per FR-3.8. Reads `Tenant.subBrandConfigJson` and seeds default cover-image colors / fonts. Q22 (brand pack) is content-blocker for actual assets — config-driven defaults are codeable now. |
| **S14** | LLM `itinerary-suggest` task class (stub-mode) | `backend/services/itinerarySuggestLLM.js` (NEW) + register in `backend/lib/llmRouter.js` + tests | ✅ DONE 2026-06-09 — `17449b35` | ~3d | Per FR-3.6. Reuses tmcDiagnosticPrompts pattern. Stub returns canned suggestion. Real-mode swap blocked on Q-IT-2 (overlaps Q11 — Gemini key). Pure module shipping is fine. |

## Block 3 — Marketing Flyer canvas editor

Per [PRD_TRAVEL_MARKETING_FLYER.md](PRD_TRAVEL_MARKETING_FLYER.md). DD-5.1 (Polotno vs in-house) gates the canvas editor itself; everything around it can ship.

| # | Slice | Files | Marker | Cost | Notes |
|---|---|---|---|---|---|
| **S15** | AI copy task class `marketing-flyer-copy` (stub-mode) | `backend/services/marketingFlyerCopyLLM.js` (NEW) + register in `lib/llmRouter.js` + tests | ✅ DONE 2026-06-10 — `866a147c` | ~1d | Per FR-3.6.1. Stub returns canned headline + body + CTA per destination. Real-mode swap blocked on Q-AI-3 (Gemini key, overlaps Q-IT-2). |
| **S16** | AI image task class `marketing-flyer-image` (stub-mode) | `backend/services/marketingFlyerImageLLM.js` (NEW) + register in `lib/llmRouter.js` + tests | ✅ DONE 2026-06-10 — `e2369919` | ~1d | Per FR-3.6.3. Stub returns a placeholder image URL. Real-mode swap blocked on Q-MF-2 (AI image API key). |
| **S17** | PDF + multi-aspect PNG render via headless Chromium (Puppeteer) | `backend/services/flyerRenderEngine.js` (NEW) + `backend/routes/travel_flyer_templates.js` (extend with `POST /:id/render`) + tests | ✅ DONE 2026-06-10 — `c128ac66` | ~3d | Per FR-3.4. Takes a flyer template + data → renders PDF (A4 + A5) + PNGs (1200×1200 square, 1080×1920 portrait IG, 1920×1080 landscape FB). Uses existing pdfRenderer pattern + adds Puppeteer for PNG path. **Shipped:** `renderFlyer({template, data, format})` engine (5-format dispatch table) + `POST /:id/render` route (ADMIN/MANAGER, sub-brand gated, X-Flyer-Render-Engine header) + 27 vitest cases + 14 Playwright API tests. **Puppeteer NOT in `backend/package.json` yet** — PNG branch returns a deterministic 1×1 stub PNG with `engine: 'stub-1x1'`; install + swap to real Chrome screenshots is a follow-up infra slice (zero caller-side change required). **A5 PDF currently renders A4-sized buffer** because `lib/flyerPdfRender.js`'s `PAPER_SIZES` table is `{a4, us_letter}` only — follow-up lib extension lets `aspect: 'a5'` thread through (out of S17 scope per file-disjoint hard limit). |
| **S18** | Public share URL + embed code for flyers | `backend/routes/travel_flyer_public.js` (NEW) + `backend/lib/flyerShareToken.js` (NEW) + `frontend/src/pages/public/FlyerView.jsx` (NEW) + tests | ✅ DONE 2026-06-10 — `<SHA>` | ~2d | Per FR-3.5.3/4. Mirrors C9 (Quote Accept Landing) — JWT-share token → public read-only flyer view + embed iframe snippet. **Shipped:** `mintShareToken({flyerId, tenantId, expiresInSec})` + `verifyShareToken` (7-day default TTL, `aud=flyer-share`, `purpose=travel-flyer-share`) + `POST /api/v1/flyers/:id/share` (ADMIN/MANAGER, sub-brand gated) returning `{shareUrl, embedCode, expiresAt}` + `GET /api/v1/flyers/public/:slug?t=<jwt>&format=<f>` (PUBLIC) streaming binary via `flyerRenderEngine.renderFlyer` + `GET /public/:slug/meta?t=<jwt>` (PUBLIC) returning metadata + `FlyerView.jsx` public landing with format switcher + download link + copy-embed-code button + embed-iframe-only mode (`?embed=1`) + 33 backend vitest cases + 9 frontend vitest cases + 14 Playwright API cases. Audit trail emits `TRAVEL_FLYER_PUBLIC_SHARE_MINTED` + `TRAVEL_FLYER_PUBLIC_RENDER` action verbs. **Server.js mount deferred** as follow-up gap row (shared-file hazard with sibling Wave 14 agents) — needs `app.use('/api/v1/flyers', require('./routes/travel_flyer_public'))` + `'/v1/flyers/public'` openPaths entry + spec into `deploy.yml`/`coverage.yml` lists. Depends on S17 (need rendered output to serve). |
| **S19** | SequenceStep.attachmentRefs flyer kind extension | `backend/prisma/schema.prisma` (extend SequenceStep additive) + `backend/cron/sequenceEngine.js` (handle flyer attachment when sending) + tests | ✅ DONE 2026-06-10 — `<SHA>` | ~1d | Per FR-3.5/AC-6.5. Existing Sequence engine handles file attachments; extend to handle `attachmentKind: 'flyer'` with flyer ID reference → render-on-send. **Shipped:** `SequenceStep.attachmentRefsJson String? @db.Text` (additive nullable) + `resolveStepAttachments(step, enrollment, channel)` helper in `cron/sequenceEngine.js` that parses the JSON array + looks up `TravelFlyerTemplate` (tenant-scoped) + calls `services/flyerRenderEngine.renderFlyer({template,data,format})` per `kind:'flyer'` ref + threads rendered buffers into `trySendGridSend()` as base64 MIME attachments + emits `writeAudit('SequenceStep','sequence.step.flyer-attached',...)` per attach + `'sequence.step.flyer-attach-failed'` per render/lookup failure. Per-attachment failure isolation (render exception / template-not-found / DB outage) — surrounding step still sends. Stub-engine PNG (when Puppeteer absent) still attached. CJS self-mocking-seam via `renderFlyerSafe` + `writeAuditSafe` exports. 21 vitest cases (`backend/test/cron/sequenceEngine-flyer-attachment.test.js`) covering schema acceptance, render-on-send happy path, multi-flyer batching, stub-engine fallback, render/lookup/audit failure isolation, tenant-scoping, file-ref passthrough, kind:'sms' channel, and processStep integration. Probe-skip e2e spec (`e2e/tests/sequence-engine-flyer-attachment-api.spec.js`) parked until a follow-up slice extends the sequence-step route to forward `attachmentRefsJson`. Depends on S17. |
| **S20** | 🔵 BLOCKED — Canvas editor (DD-5.1 — Polotno embed vs in-house) | `frontend/src/pages/travel/FlyerCanvasEditor.jsx` (NEW) + many | 🔵 BLOCKED | ~5-10d depending on choice | **Blocked on:** DD-5.1 product call. Polotno embed = ~5d (license cost). In-house = ~10d (more flex, no license). |
| **S21** | 🔵 BLOCKED — Asset library + multer pipeline + tag search | `backend/routes/travel_flyer_assets.js` (NEW) + Multer S3/Cloudinary uploader + frontend modal | 🔵 BLOCKED | ~3d | **Blocked on:** Q-MF-1 (storage cred — S3 vs Cloudinary credentials). |
| **S22** | 🔵 BLOCKED — WhatsApp share flow | `backend/services/flyerWhatsAppShare.js` (NEW) | 🔵 BLOCKED | ~1d | **Blocked on:** Q9 (Wati WhatsApp Business creds — overlaps with broader 7-cron WA stubs). |

## Block 4 — B2B Agent Portal

Per [PRD_TRAVEL_B2B_AGENT_PORTAL.md](PRD_TRAVEL_B2B_AGENT_PORTAL.md). Entire PRD is BLOCKED on 7 design decisions that gate impl topology. **No agent can dispatch on these until the DDs are resolved.** Listed here for transparency.

| # | Slice | Marker | Why blocked |
|---|---|---|---|
| **S23** | 🔵 SubAgent model + auth + dashboard | 🔵 BLOCKED | DD-5.1 (new app vs route prefix) |
| **S24** | 🔵 Commission ledger + accrual + monthly statement + TDS | 🔵 BLOCKED | DD-5.5 (approval chain shape) |
| **S25** | 🔵 Markup / clone-with-margin for sub-agents | 🔵 BLOCKED | DD-5.4 (policy editor surface) |
| **S26** | 🔵 CorporateAccount + multi-traveler booking | 🔵 BLOCKED | DD-5.5 |
| **S27** | 🔵 Travel-policy validator + approval workflow | 🔵 BLOCKED | DD-5.4 + DD-5.5 |
| **S28** | 🔵 Expense reporting + CSV/JSON/PDF exports | 🔵 BLOCKED | DD-5.5 |
| **S29** | 🔵 Per-sub-brand theming + AGENT RBAC extension | 🔵 BLOCKED | DD-5.1 |

## Block 5 — Smaller per-PRD residuals (codeable today)

| # | Slice | Files | Marker | Cost | Notes |
|---|---|---|---|---|---|
| **S30** | Quote → Invoice convert flow | `backend/routes/travel_quotes.js` (extend with `POST /:id/convert-to-invoice`) + `backend/lib/quoteToInvoiceConverter.js` (NEW) + tests | ✅ DONE 2026-06-09 — `8dc56a70` (marker flip; impl already shipped as `434b68a1` #900 slice 10) | ~2d | Per [PRD_TRAVEL_QUOTE_BUILDER](PRD_TRAVEL_QUOTE_BUILDER.md). `TravelInvoice.quoteId` FK already exists. Copy lines + amounts to invoice, status: `Draft` (per FR-3.9.1, NOT `pending_payment` as originally tracked — operator reviews before sending). Idempotent — re-convert returns existing invoice with `ALREADY_CONVERTED` code. Implemented inline in `routes/travel_quotes.js:2488-2638` rather than as a separate `quoteToInvoiceConverter.js` lib (mirror of the `nextInvoiceNum` inline pattern in `travel_invoices.js`; both produce same contract). Tests pinned in `backend/test/routes/travel-quotes-convert-to-invoice.test.js` (8 cases: RBAC denied + INVALID_ID + cross-tenant 404 + sub-brand denied + happy path 201 + idempotency 200 + zero-lines + dueDate +30d). |
| **S31** | `QuoteTemplate` model + admin page + CRUD route | `backend/prisma/schema.prisma` (additive `TravelQuoteTemplate` model) + `backend/routes/travel_quote_templates.js` (NEW) + `frontend/src/pages/travel/QuoteTemplates.jsx` (NEW) + tests | ✅ DONE 2026-06-09 — `8fb23237` | ~2d | Per [PRD_TRAVEL_QUOTE_BUILDER](PRD_TRAVEL_QUOTE_BUILDER.md). Template = pre-filled QuoteLine set for common itineraries (Umrah-7d / Golden-Triangle-5d / etc.). Apply to new quote. Ships `TravelQuoteTemplate` Prisma model (additive, no bless marker — fresh model with `tenantId`/`name`/`description?`/`subBrand?`/`category?`/`currency`/`linesJson` (JSON array)/`isActive` + indexes on `(tenantId, isActive)` and `(tenantId, subBrand, isActive)`), 6-endpoint CRUD route (GET list + POST + GET :id + PATCH :id + DELETE :id soft + POST :id/apply with 409 ALREADY_HAS_LINES idempotency strategy), `QuoteTemplates.jsx` admin page (filter bar by sub-brand/category/active + create/edit form with JSON line editor), 32 backend vitest + 21 frontend RTL cases. Follow-up gaps: (a) **server.js mount** of `/api/travel/quote-templates` router (deferred — server.js is reserved for separate slice); (b) **App.jsx route registration** at `/travel/quote-templates` with React.lazy() import (deferred — App.jsx is reserved for separate slice); (c) **Sidebar nav entry** under Travel admin section for /travel/quote-templates. |
| **S32** | FX-rate locking at accept-time | `backend/routes/travel_quotes_public.js` (extend accept handler to snapshot FX rate) + tests | ✅ DONE 2026-06-09 — `5693f3ee` | ~1d | Per [PRD_TRAVEL_QUOTE_BUILDER](PRD_TRAVEL_QUOTE_BUILDER.md). On accept, capture FX rate to a snapshot field on TravelQuote. Prevents margin shift between quote send + customer accept. FX-lock data persisted inside `TravelQuoteSnapshot.snapshotJson` (freeform JSON field already written on every customer transition) — no schema column add. Future S47 candidate: promote `fxLock` to dedicated columns (`fxRateSnapshot`, `fxRateLockedAt`) once a schema-add slice is dispatched; the existing `readFxLockFromAcceptSnapshot()` helper is the migration read-path for pre-migration rows. Idempotent: re-accept on Already-Accepted quote returns 409 ALREADY_ACTIONED carrying the originally-locked rate (no fresh tenant/currency lookup). FX-rate source: `Currency.exchangeRate` for `(quote.currency, quote.tenantId)` → tenant `defaultCurrency`; fail-soft on missing/error (rate=null + reason; accept still 200). Tests: +9 vitest cases (24/24 green). |
| **S33** | CancellationPolicy model + CR-NOTE issuance flow | `backend/prisma/schema.prisma` (CancellationPolicy + CreditNote shapes) + `backend/routes/travel_invoices.js` (extend with CR-NOTE issuance) + tests | ✅ DONE 2026-06-09 — `1614f88e` | ~3d | Per [PRD_TRAVEL_BILLING](PRD_TRAVEL_BILLING.md). When invoice marked `cancelled`, auto-create credit-note row per policy table (full-refund < N days, 50% refund N-M days, no-refund < M days). Parent-child credit-note FK already exists. SHIPPED: (a) `CancellationPolicy` Prisma model (additive — `tenantId`, `name`, `subBrand?` (null = tenant-wide default), `description?`, `tiersJson` (LongText JSON array `[{daysBeforeServiceStart, refundPercent}]`), `isActive`; `@@unique([tenantId, name])` + `(tenantId, isActive)` + `(tenantId, subBrand, isActive)` indexes); (b) nullable `cancellationPolicyId Int?` FK on `TravelInvoice` + `@@index([tenantId, cancellationPolicyId])`; (c) `POST /invoices/:id/void` extended with `resolveCancellationOutcome()` tier-walker → auto-creates `docType=CreditNote` row with `parentInvoiceId` set, `totalAmount = -refundAmount`, audit `autoIssued:true` + tier metadata; response envelope additively gains `creditNote` + `policyApplied` fields (back-compat preserved for existing destructurers); (d) new CRUD route `backend/routes/travel_cancellation_policies.js` (GET list / GET :id / POST / PATCH / DELETE; ADMIN-only on DELETE; sub-brand-scoped; canonicalises tier sort DESC at write); (e) 36 vitest cases for CRUD + 10 vitest cases extending the void test for the auto-issuance tier matrix (full-refund / partial / no-refund / no-service-date / no-policy / draft-skip / CN-parent-skip / audit trail / pinned-policy-override / defensive-error-isolation). **Lookup precedence**: invoice.cancellationPolicyId → sub-brand-default → tenant-wide-default; first match wins. **Days-before-start** computed from MIN(invoiceLines.serviceStartDate). Migration-safety GREEN (additive nullable, no bless marker needed). NO server.js mount + NO App.jsx route + NO Sidebar entry — server.js + frontend wiring are reserved follow-up slices. |
| **S34** | Per-sub-brand PDF invoice templates | `backend/services/pdfRenderer.js` (extend `renderInvoice()` with sub-brand-aware template selection) + tests | ✅ DONE 2026-06-09 — `3653795f` | ~2d | Per [PRD_TRAVEL_BILLING](PRD_TRAVEL_BILLING.md). Reads `Tenant.subBrandConfigJson` for per-sub-brand headerColor / primaryColor / accentColor / fontFamily / thumbnailUrl. Same shape + verbatim hex fallbacks as S13 (cross-doc consistency: an admin who configures one sub-brand block once gets matching colors across both invoice PDFs + itinerary templates). Q22 blocker is content (actual logo URL + final hex codes); template-selection logic shipped. Wellness invoices (renderBrandedInvoicePdf path) untouched — back-compat preserved. Tests: NEW `backend/test/services/travel-invoice-pdf-brand-kit.test.js` (29 vitest cases: 7 parser + 11 resolver + 10 renderer + 1 wellness-back-compat). 7 BRAND_KIT_FALLBACKS hex values pinned VERBATIM against S13. Follow-up surfaced: (a) BrandKit model migration (DD-5.2) — currently subBrandConfigJson is freeform; once a typed BrandKit Prisma model lands, the selector should add BrandKit as precedence layer 2.5 (above hard-coded fallback, below per-sub-brand subBrandConfigJson block); (b) logo URL fallback strategy — `branding.thumbnailUrl` is plumbed end-to-end but `renderTravelInvoicePdf` doesn't yet embed the logo image in the header band (requires `pdfkit doc.image()` + a download/cache layer for remote URLs); (c) sibling travel PDF helpers (itinerary / quote / diagnostic / tmc-readiness / travelstall-personalised) haven't adopted the brand kit yet — they still use the literal SUB_BRAND_ACCENT hex constants. Pre-requisites: Q22 brand pack landing OR DD-5.2 BrandKit model decision. |
| **S35** | Express static-file middleware for `__CSP_NONCE__` substitution | `backend/middleware/cspNonceSubstitution.js` (NEW) + `backend/server.js` (mount before static handler) + tests | ⬜ TODO | ~2h | Flagged by S1 agent: `frontend/index.html` carries `__CSP_NONCE__` placeholder; today it's inert. Need a middleware that reads served index.html, substitutes the placeholder with `res.locals.cspNonce` per response, AND stamps the matching nonce onto every `<script>`/`<style>` tag's `nonce` attribute. Once shipped, the CSP can flip from Report-Only to enforce. Alternative path is Nginx `sub_filter` (deploy.yml + Nginx config — out of cron scope). Depends on S1 ✅. **Parallel-safe** with S3 + S4 + S8-S19 + S30-S34 + S36. |
| **S36** | Tenant-scope fixes for 5 reviewer-confirmed callsites | `backend/routes/approvals.js` (line 38) + `backend/routes/auth.js` (lines 109 + 350) + `backend/routes/dashboards.js` (lines 335 + 379) + `backend/routes/reports.js` (lines 200 + 201) + tests | ✅ DONE 2026-06-10 — `5be509de` | ~3h | Flagged by S2 agent: 5 callsites surfaced by the new ESLint heuristic plugin are GENUINE tenant-scope leaks (vs ~64 catalogued false-positive patterns like by-id-after-tenant-fetch or ADMIN-only verifyRole-guarded). Per-route fix = wrap each Prisma call in `tenantWhere()` or add explicit `where.tenantId = req.user.tenantId` clause. Pin via per-route cross-tenant probe in the e2e gate spec. Depends on S2 ✅ (audit doc identifies them). Closes the GENUINE tail of #919. **Parallel-safe** with S3 + S4 + S8-S19 + S30-S35. **S36 disposition (2026-06-10):** Of the 5 reviewer-flagged callsites, only `approvals.js:38` was a GENUINE leak (hydrateUsers User lookup unscoped — fixed: pass + use `tenantId` in the user findMany). The other 4 (auth.js:109/353, dashboards.js:335 + 379, reports.js:200-203) surfaced as spread-pattern / public-route false-positives — auth.js:109 + 353 are PUBLIC `/auth/public/tenants` + `/auth/customer/tenants` with no `req.user.tenantId`; dashboards.js:335 + 379 spread `...tenantWhere`; reports.js:200-203 (4 lines, not 2 — ESLint actually flagged L200-203) spread `...baseWhere` from buildWhere() helper. All 4 now carry inline `// eslint-disable-next-line gbscrm/tenant-scope-finder-heuristic` with rationale. ESLint count on the 4 touched files: 8 warnings → 0. 5 new e2e probes added to `cross-tenant-coverage-audit.spec.js`. Audit doc updated. |
| **S37** | Composite index `@@index([itineraryId, dayNumber])` on ItineraryItem | `backend/prisma/schema.prisma` (additive index — no bless marker, indexes are non-destructive) + migration safety + tests | ✅ DONE 2026-06-09 — `5253dbc0` | ~½h | Flagged by S8 agent: the existing `@@index([itineraryId, position])` won't be hit when S9's visual editor queries `WHERE itineraryId = X AND dayNumber = Y ORDER BY position` for day-filtered reads. Add the composite index now or fold into S9's PR if file-disjoint allows. Depends on S8 ✅. **Parallel-safe** with S3 + S4 + S10-S19 + S30-S36 (NOT with S9 — both touch the same schema model; serialise if both queued). |
| **S38** | Wire `allowIframeEmbedding({ allowList: ['*'] })` into `/embed/lead-form.html` handler | `backend/server.js` (mount route-level middleware before static handler for `/embed/*`) + extend `e2e/tests/public-booking-api.spec.js` (or new spec) | ✅ DONE 2026-06-09 — `7a229c92` | ~½d | Flagged by S4 agent: S4's new global `X-Frame-Options: DENY` + `frame-ancestors 'none'` defaults will now block partner-site iframe loads of the embed widget. The `allowIframeEmbedding()` factory was shipped in S4 but the consumer wire-in was outside slice scope (server.js was not in S4's allowed-files column). Single-line mount + test pin. Depends on S4 ✅. **Parallel-safe** with S3 + S9-S19 + S30-S37 + S39 + S40. **Shared-file hazard:** touches `backend/server.js`. |
| **S39** | Schema add — `Tenant.embedAllowlistJson String? @db.Text` + wire `readTenantEmbedAllowlist()` to real read | `backend/prisma/schema.prisma` (additive nullable, no bless marker) + `backend/middleware/security.js` (replace stub-null with real Prisma read + JSON.parse) + tests | ✅ DONE 2026-06-10 — `56832537` | ~½d | Flagged by S4 agent: `readTenantEmbedAllowlist()` ships in S4 as a stub returning null because `Tenant.embedAllowlistJson` doesn't exist yet. Schema add is additive nullable; the existing unit test pins null + asserts the next author updates the test. Depends on S4 ✅. **Parallel-safe** with S3 + S9-S19 + S30-S36 + S38 + S40 (NOT with S37 — both touch schema.prisma; serialise). SHIPPED: schema column added (the new `embedAllowlistJson String? @db.Text` column lands as additive nullable — migration-safety detector clean, no bless marker needed); `readTenantEmbedAllowlist(prisma, tenantId)` in `backend/middleware/security.js` flipped from the S4 stub-returning-null to a real `prisma.tenant.findUnique({ select: { embedAllowlistJson: true } })` → `JSON.parse` → `Array.isArray` chain. Returns null on null column / malformed JSON (catch-and-warn, never throws) / non-array JSON / missing inputs. Signature `(prisma, tenantId)` preserved verbatim — no caller-side updates needed. `backend/test/middleware/security-csp.test.js` extended 13 → 18 tests with 5 new pins: parsed-array happy-path, null-column, malformed-JSON, non-array (object / string / number), short-circuit. Vitest green. **Follow-up gap surfaced for S38 mount:** server.js currently mounts `app.use("/embed", allowIframeEmbedding({ allowList: ["*"] }))` (commit `7a229c92`) — now that the per-tenant reader is wired, that wildcard can be swapped for a per-tenant read on the next slice (NOT in S39 scope — server.js outside the Files column). |
| **S40** | Wire `e2e/tests/security-headers-api.spec.js` into deploy.yml + coverage.yml gate-spec lists | `.github/workflows/deploy.yml` + `.github/workflows/coverage.yml` | ✅ DONE 2026-06-09 — `bc0ccbe2` | ~5min | Flagged by S4 agent: spec is in tree but not gated per-push. Workflow files were outside slice S4 file scope. Tiny 2-file ops fix per the `wiring-spec-into-gate` skill. Depends on S4 ✅. **Parallel-safe** with everything else. SHIPPED: spec line `tests/security-headers-api.spec.js \` inserted in BOTH deploy.yml (line 484) + coverage.yml (line 312) immediately before `tests/teardown-completeness.spec.js` per the `wiring-spec-into-gate` skill (trailing-backslash gotcha c8a8ad4 avoided). Inventory comment block in deploy.yml extended with the new spec's tag (8 tests, #921 slice S4 FR-3.6 iframe-isolation — `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'` + SRI build tags) and the total-tests tally bumped 1725 → 1733. coverage.yml has no inventory comment block (intentional — coverage.yml only lists the bash-invocation; deploy.yml is the canonical inventory). Spec was confirmed in-tree via `ls`. CI will exercise the spec on the next push. |
| **S41** | 🔵 BLOCKED — PRD FR-3.5(a) "summary-as-default" cross-cutting flip | `backend/lib/listProjection.js` (flip default direction) + 8 routes shipped in S3 (drop `?fields=summary` gate) + 51 prior `?fields=summary` slice routes + every frontend consumer page (opt OUT to `?fields=full` per-page) + every e2e + RTL spec | 🔵 BLOCKED | ~3-5d | Flagged by S3 agent: PRD FR-3.5(a) direction is summary-by-default + `?fields=full` opt-out. S3 (and the 51 prior `?fields=summary` slices 1-51) shipped the opposite (slim-on-opt-in) to preserve back-compat. **Blocked on:** DD-5.4 (per-endpoint hand-curated vs global middleware — PRD §FR-3.5 open question). **Unblocked when:** product call decides flip direction + frontend coordinator wave is scheduled. Until then, every new list endpoint should follow the S3 opt-in pattern for consistency. |
| **S42** | Wellness PHI list-endpoint slim projection | `backend/routes/wellness.js` (Patient + Visit + Prescription list endpoints) + `backend/lib/listProjection.js` (add Patient/Visit/Prescription registry entries) + `backend/middleware/phiReadGate.js` (coordinate with existing PHI gates) + tests | ✅ DONE 2026-06-09 — `2c42a0ed` | ~2d | Flagged by S3 agent: wellness PHI list endpoints (`GET /patients`, `GET /visits`, `GET /prescriptions`) still ship full PHI (phone/email/dob/allergies/notes @db.Text) by default. Same slim-projection pattern as S3 but needs `phiReadGate` middleware coordination + audit-log integration since PHI reads are HIPAA-regulated (must record who-read-what-when on detail-shape reads). Depends on S3 ✅. **Parallel-safe** with everything except concurrent S3-pattern adoptions. **Shared-file hazard:** none — wellness.js is independent. SHIPPED: (a) `listProjection` registry extended with Patient / Visit / Prescription entries — slim shapes drop every PHI column at the Prisma `select` layer (phone/email/dob/allergies/notes/drugs/instructions/vitals/photos/home-address/etc.); (b) `?fields=summary` opt-in wired into the three GET handlers — default shape stays full PHI (back-compat with 28 frontend wellness pages that destructure `patient.phone` etc.); (c) audit-coordination contract — {PATIENT,VISIT,PRESCRIPTION}_LIST_READ row writes `shape: 'full'\|'summary'` so reviewers can answer "did this op leak PHI?" without joining tables; PII_DISCLOSED row SKIPPED on slim path (no PII columns in response = no disclosure event); `shouldMaskForViewer` masking still applies to `name` on slim path for low-trust viewers (telecaller / generic-USER with RBAC permission grant); (d) NEW `backend/middleware/phiReadGate.js` exports `makePhiReadGate()` factory — canonical surface for future routes; the existing inline `phiReadGate` const in `routes/wellness.js` (25+ callsites) is UNCHANGED (cross-cutting rename deferred to its own slice). Test surface delta: `backend/test/lib/listProjection.test.js` 41 → 48 cases (+ Patient/Visit/Prescription PII-absence + shape-pinning); `e2e/tests/wellness-clinical-api.spec.js` +9 cases (slim PHI absence × 3 endpoints + default full-shape × 3 + case-strict `?fields=` + audit shape persistence + helper-role gate-still-applies). **Follow-up gaps:** (1) extract inline `phiReadGate` const from `wellness.js` to `require('../middleware/phiReadGate').makePhiReadGate()` — 25-callsite rename in a single file (cross-cutting slice); (2) if a future privacy review classifies even mask-applied `name` on slim path as too-PII for default-disclosure, the slim shape needs a mask-by-default flip (PRD-level decision); (3) the e2e audit-shape pin is soft (audit list endpoint shape varies per deployment); strict pin needs a dedicated audit-shape-contract slice. |
| **S43** | Slim projection for `routes/travel_visa.js GET /applications` | `backend/routes/travel_visa.js` (extend list endpoint with `?fields=summary` gate; suppress the `.map(a => ({...a, contact}))` decoration on slim path) + `backend/lib/listProjection.js` (add VisaApplication registry entry) + extend `e2e/tests/travel-visa-api.spec.js` | ✅ DONE 2026-06-10 — `bd7e4b13` | ~½d | Flagged by S3 agent: visa-applications list has a contact-decoration pattern that mixes the VisaApplication row with contact PII. Slim adoption needs the decoration to be SKIPPED on the slim path (same shape as marketplace_leads' `contact` include skip in S3). Depends on S3 ✅. **Parallel-safe** with everything else. |
| **S44** | 🔵 BLOCKED — Real-mode wire-in of `callGemini` in `itinerarySuggestLLM.js` | `backend/services/itinerarySuggestLLM.js` (single-file swap — TODO block already laid out with `@google/generative-ai` dispatch sketch) + tests | 🔵 BLOCKED | ~½d | Flagged by S14 agent: stub-mode S14 shipped with a real-mode TODO block. **Blocked on:** Q-IT-2 / Q11 (`GEMINI_API_KEY` from Yasin — same cred unlock as 7 other LLM stubs). **Unblocked when:** Yasin provisions the Gemini key. Until then, every itinerary-suggest call returns the canned stub shape. Depends on S14 ✅. |
| **S45** | Per-tenant LLM key support via `SupplierCredential` category `"llm-key"` | `backend/lib/llmRouter.js` (new `getLlmKey()` helper — SupplierCredential `category='llm-key'` → ENV fallback; `llmEnabled` flipped sync → async) + `backend/services/itinerarySuggestLLM.js` (`realModeEnabled` flipped sync → async, delegates to `llmRouter.getLlmKey`) + tests | ✅ DONE 2026-06-10 — `dd654e4f` | ~2d | Flagged by S14 agent: PRD §9.1 plans per-tenant LLM credentials via `SupplierCredential` category `"llm-key"`. **Shipped (S45):** llmRouter `getLlmKey(tenantId, model)` checks `prisma.supplierCredential.findFirst({ where: { tenantId, category: 'llm-key', supplierName: { in: [model, envVar] } } })` first then falls back to `process.env[envVar]`; AES-256-GCM ciphertext decrypted via existing `lib/fieldEncryption.decrypt()`; Prisma errors log + treat as miss (never throw). `llmEnabled(task, tenantId?)` is now async — caller-cascade audit found ZERO production callers outside its own tests (no `await` cascade needed). `itinerarySuggestLLM.realModeEnabled(tenantId?)` async-flipped + delegates to `getLlmKey` so the cascade lives in one place. **Out of scope (follow-ups):** adsGpt / ratehawk / callified have NO `llmEnabled` function — they use their own integration categories (`adsgpt-key` / `ratehawk-cred` / `ai-calling-key`), separate slices. `tmcDiagnosticPrompts.js` is a pure prompt-builder (no env check, no work). `marketingFlyerCopy` / `marketingFlyerImage` do not exist (the slice listed them speculatively). Depends on S14 ✅. **Parallel-safe** with everything except concurrent llmRouter edits. |
| **S46** | `POST /api/travel/itineraries/suggest` route handler — surface S14 to the frontend | `backend/routes/travel_itineraries.js` (extend with new POST handler that calls `suggestItinerary()` and returns suggestionJson) + extend `e2e/tests/travel-itineraries-api.spec.js` | ✅ DONE 2026-06-09 — `74406110` | ~½d | Flagged by S14 agent: S14 shipped the service module but no route handler exists to call it. FR-3.6 step (a) describes a "Suggest itinerary" button on `Itineraries.jsx` — the button needs an endpoint. Depends on S14 ✅. **Parallel-safe** with everything except concurrent travel_itineraries.js edits. Follow-up gap: frontend "Suggest itinerary" button on `frontend/src/pages/travel/Itineraries.jsx` + integration into the visual editor blank-state (S9) is a separate slice. |
| **S47** | Promote `fxLock` from `TravelQuoteSnapshot.snapshotJson` to dedicated columns on TravelQuote | `backend/prisma/schema.prisma` (additive nullable — `fxRateSnapshot Decimal(15,6)?` + `fxRateTargetCurrency String?` + `fxRateLockedAt DateTime?`; no bless marker) + `backend/routes/travel_quotes_public.js` (write to new columns; keep `readFxLockFromAcceptSnapshot()` as the pre-migration read fallback) + extend `e2e/tests/travel-quotes-public-api.spec.js` | ⬜ TODO | ~½d | Flagged by S32 agent: S32 persisted fxLock inside the freeform `TravelQuoteSnapshot.snapshotJson` because schema.prisma was outside slice scope. Dedicated columns let aggregate-level reporting + downstream consumers query the locked rate directly without parsing JSON. Migration is additive nullable; the existing helper becomes the read-fallback for pre-S47 quotes. Depends on S32 ✅. **Parallel-safe** with everything except concurrent schema.prisma slices (serialise with S37/S39 if queued). **Note:** this row was lost in the Wave 4 parallel-agent docs collision and re-added in commit `<recovery-sha>`. |
| **S48** | Wire `/api/travel/quote-templates` router into `backend/server.js` | `backend/server.js` (mount route below other travel routes — 1-line `app.use("/api/travel/quote-templates", require("./routes/travel_quote_templates"));`) + integration smoke test | ✅ DONE 2026-06-09 — `32630ec1` | ~30min | Flagged by S31 agent: S31 shipped `routes/travel_quote_templates.js` but did not touch `server.js` (shared-file hazard per cron rules). Without this mount, the 6 endpoints are inert. Depends on S31 ✅. **Shared-file hazard:** touches `backend/server.js`. **Parallel-safe** with everything not also touching server.js (e.g. S38 also touches server.js — serialise). |
| **S49** | App.jsx route registration + Sidebar nav entry for `QuoteTemplates.jsx` | `frontend/src/App.jsx` (add lazy-import + `<Route path="/travel/quote-templates" ...>` registration) + `frontend/src/components/Sidebar.jsx` (add nav entry under Travel admin section) + tests | ✅ DONE 2026-06-10 — `892f080f` (combined S49+S55) | ~30min | Flagged by S31 agent: S31 shipped `frontend/src/pages/travel/QuoteTemplates.jsx` but did not touch `App.jsx` or `Sidebar.jsx` (both reserved as shared-file hazards). Without these wirings, the page is unreachable from the running app. Depends on S31 ✅. **Shared-file hazard:** touches `frontend/src/App.jsx`. **Parallel-safe** with everything not also touching App.jsx. **Combined-slice rationale (S49+S55):** both rows touch the same two shared-file hazards (App.jsx + Sidebar.jsx); dispatching as a single sweep avoided the serialise overhead. **Shipped:** App.jsx lazy import `TravelQuoteTemplates = lazy(() => import("./pages/travel/QuoteTemplates"))` adjacent to TravelItineraryTemplates + `<Route path="travel/quote-templates" element={<TravelOnly><TravelQuoteTemplates /></TravelOnly>} />` adjacent to the existing travel/itinerary-templates Route. Sidebar.jsx new `FileStack` icon import + `isManager`-gated `<Link to="/travel/quote-templates" icon={FileStack} label="Quote Templates" />` in the Quotes cluster (under Quote Builder). Test surface delta: Sidebar.test.jsx +3 cases (ADMIN sees /href + MANAGER sees /href + USER hidden) + QuoteTemplates.test.jsx +1 case (App.jsx route registration pin via static-grep against `../App.jsx` source). |
| **S50** | e2e Playwright API spec for `travel-quote-templates` + wire into deploy.yml + coverage.yml gate-spec lists | `e2e/tests/travel-quote-templates-api.spec.js` (NEW) + `.github/workflows/deploy.yml` + `.github/workflows/coverage.yml` (add spec to gate list) | ⬜ TODO | ~½d | Flagged by S31 agent: per the standing rule for new routes ("New route handler → add an API spec... wire into BOTH deploy.yml and coverage.yml spec lists"). The 32 vitest cases cover the contract; the e2e spec adds a per-push CI gate. Depends on S31 ✅. **Parallel-safe** with everything except concurrent workflow-file edits (S40 also touches workflows — serialise or batch). |
| **S51** | Logo URL embedding in travel invoice PDFs via `pdfkit doc.image()` | `backend/services/pdfRenderer.js` (extend `renderTravelInvoicePdf` header band with logo image fetch + embed) + remote-URL download/cache layer (axios + filesystem cache OR Cloudinary CDN dispatch) + tests | ✅ DONE 2026-06-09 — `19b7bc31` | ~1d | Flagged by S34 agent: `branding.thumbnailUrl` is plumbed end-to-end (parser → resolver → renderer) but the renderer doesn't yet `doc.image()` the logo into the header band. The download/cache layer is the bulk of the work. Engineering can ship with a fixture URL today; real Yasin logos via Q22 swap in later. Depends on S34 ✅. **Parallel-safe** with everything except concurrent `pdfRenderer.js` edits. **S51 close:** Inline `fetchLogoBuffer(url, opts?) → Promise<Buffer\|null>` in pdfRenderer.js with axios + in-memory LRU `Map` cache (max 50 entries, FIFO eviction, TTL 1h, HTTP timeout 5s, max 5MB). Fail-soft on any error (404/timeout/non-200/malformed). `renderTravelInvoicePdf` now `async`, calls the seam via `module.exports.fetchLogoBuffer(...)` (CJS self-mock seam), embeds via `doc.image(buf, x, y, { fit: [80, 40] })` in the header band's top-right. doc.image() wrapped in try/catch so a malformed buffer never 500s the download. Back-compat: when `branding.thumbnailUrl` is null (still the default until Q22 logos land), renderer behaviour is byte-shape-identical to S34 output. 7 new vitest cases extending `backend/test/services/travel-invoice-pdf-brand-kit.test.js` (S51 a/b/c/d + TTL expiry + null-URL contract + axios-throws contract). 81 total pdfRenderer-test cases stay green. |
| **S52** | Brand-kit adoption sweep for sibling travel PDF helpers | `backend/services/pdfRenderer.js` (replace literal `SUB_BRAND_ACCENT` hex constants in `renderTravelItineraryPdf`, `renderTravelQuotePdf`, `renderTravelDiagnosticPdf`, `renderTmcReadinessReport`, `renderTravelStallPersonalisedPdf` with the S34 selector) + tests | ✅ DONE 2026-06-10 — `7b4b123c` | ~1d | Flagged by S34 agent: 5 sibling travel PDF helpers still use literal hex constants. Sweep adopts the S34 selector pattern so a single `subBrandConfigJson` admin POST cascades into ALL travel-vertical PDFs. Depends on S34 ✅. **Parallel-safe** with everything except concurrent `pdfRenderer.js` edits (S51 also touches pdfRenderer.js — serialise). **S52 close:** Generic-named aliases `parseTravelSubBrandConfig` / `resolveTravelBrandKit` exported alongside the S34 invoice-specific names (back-compat with S34 test surface). New shared helper `resolveTravelHeaderBrandKit(subBrand, opts)` returns `{ branding, source }` for one-call use inside each renderer. All 5 sibling renderers now read `branding.headerColor` from the shared selector; pre-S52 they read `SUB_BRAND_ACCENT[sub]`. **Intentional color shift**: the four travel sub-brands now share the S13-aligned palette (tmc #1F4E79, rfu #0B5345, travelstall #922B21, visasure #283747) so admin-curated brand-kit edits to `subBrandConfigJson` cascade uniformly across invoice / itinerary / quote / diagnostic / tmc-readiness / travelstall-personalised PDFs. Legacy single-arg / multi-arg call sites keep working (opts defaults to `{}`). `renderTravelQuotePdf` preserves the pre-S52 `quote.brandKit.accent` inline-override (precedence layer 1). Wellness invoices (`renderBrandedInvoicePdf` path) byte-shape unchanged — pinned via assertion in the new test file. Tests: NEW `backend/test/services/travel-sibling-pdfs-brand-kit.test.js` (30 vitest cases covering: 5 alias-surface + 4 itinerary + 5 quote + 5 diagnostic (incl. `logoBuffer` pre-S52 contract) + 5 tmc-readiness + 4 travelstall-personalised + 1 wellness-back-compat + 1 with malformed JSON). All 36 S34 brand-kit + 209 broader PDF tests + 756 backend-service tests stay green. Follow-up surfaced: (a) `renderTravelQuotePdf` body still calls `(q.brandKit && q.brandKit.logoUrl)` to print a `[Logo: <url>]` text placeholder — once S51-style `doc.image()` logo embedding ports to the quote renderer, that placeholder should be replaced with a real image embed using the brand-kit-resolved `branding.thumbnailUrl`; (b) `renderTravelStallPersonalisedPdf` and `renderTravelItineraryPdf` similarly don't embed logo images — they're plumbed for `branding.thumbnailUrl` via the resolver but the `doc.image()` embed only landed in `renderTravelInvoicePdf` in S51. Follow-up cron-pickable as "S52a: extend S51 logo-embed pattern to the 4 sibling renderers"; (c) `SUB_BRAND_ACCENT` and `SUB_BRAND_LABEL` constants are still defined in module scope but no longer read by any travel PDF renderer for header color — left in place defensively in case a future caller relies on them, but could be dropped once we audit downstream consumers; (d) the new color palette will visibly differ from prior travel PDFs on the demo box (e.g. TMC header was #0B4F6C teal, now #1F4E79 navy) — Yasin's Q22 brand pack still trumps everything via `subBrandConfigJson` admin POST when it lands. |
| **S53** | Wire `/api/travel/cancellation-policies` router into `backend/server.js` | `backend/server.js` (mount route below other travel routes — 1-line `app.use("/api/travel/cancellation-policies", require("./routes/travel_cancellation_policies"));`) | ✅ DONE 2026-06-09 — `7e6a98b1` | ~30min | Flagged by S33 agent: S33 shipped `routes/travel_cancellation_policies.js` but did not touch server.js (shared-file hazard per cron rules). **URGENT** — `backend/test/integration/route-mount-audit.test.js` will fire RED on the next push if the mount isn't added. Same recovery pattern as S48 (S31's `quote-templates` mount). Depends on S33 ✅. **Shared-file hazard:** touches `backend/server.js`. **Parallel-safe** with everything not also touching server.js (S38 + S48-done also touch server.js — serialise). |
| **S54** | Frontend admin UI `CancellationPolicies.jsx` for tenant admins to configure policies | `frontend/src/pages/travel/CancellationPolicies.jsx` (NEW — mirror S31 QuoteTemplates.jsx pattern: filter bar + create/edit form with JSON tier editor + tier preview "at 30+d → 100% refund; at 7-29d → 50%; at <7d → 0%") + RTL tests | ✅ DONE 2026-06-10 — `4823b160` | ~1d | Flagged by S33 agent: S33 shipped backend CRUD but no admin UI exists. Mirror the S31 QuoteTemplates.jsx pattern. Depends on S33 ✅. **Parallel-safe** with everything except concurrent QuoteTemplates pattern changes. **S54 close:** `frontend/src/pages/travel/CancellationPolicies.jsx` (NEW, ~720 LOC) — mirrors the S31 QuoteTemplates.jsx pattern with CancellationPolicy-specific surface: sub-brand filter (+ "Tenant-wide" `_tenant` option per backend `?subBrand=_tenant` convention) + active filter (no category — CancellationPolicy has none) + create/edit modal with structured tier-row editor (each row = `{daysBeforeServiceStart, refundPercent}` numeric inputs + Remove button + "Add tier" CTA) + LIVE tier-preview band-string updated via `useMemo` on every keystroke. Default form ladder = `[{60d→100%}, {30d→50%}, {7d→25%}, {0d→0%}]` matching the TMC Default seed (S57). DELETE flow is ADMIN-only per backend `verifyRole(["ADMIN"])` (MANAGER sees Edit but NOT Trash). Backend response shape `{policies, total, limit, offset}` consumed (different from QuoteTemplates' `{items, ...}` envelope). DELETE consumes 204 No Content. Pure helpers `validateTiers()` + `renderTierPreview()` exported for unit-test reuse. **Tier preview rendering verbatim** for the default ladder: `"at 60+d → 100% refund; at 30-59d → 50% refund; at 7-29d → 25% refund; at <7d → 0% refund"` — band semantics: top tier is `Nd+`, middle tiers are `Nd-(prev-1)d`, bottom tier is `<prev-d`. Tests: NEW `frontend/src/__tests__/CancellationPolicies.test.jsx` (32 vitest cases — page chrome ×3 roles, load lifecycle ×4, filter behavior ×1, row rendering ×5 incl. preview band-string, new-policy modal ×5, edit/delete ×4, pure-helper coverage ×10). Stable `notifyObj` reference + real `travelSubBrand.js` import per RTL standing rules. Follow-up surfaced: S55 (App.jsx route registration + Sidebar nav entry — separate slice; touches the shared App.jsx + Sidebar.jsx files reserved for that slice). |
| **S55** | App.jsx route registration + Sidebar nav entry for `CancellationPolicies.jsx` | `frontend/src/App.jsx` (lazy-import + Route) + `frontend/src/components/Sidebar.jsx` (nav entry under Travel admin) + tests | ✅ DONE 2026-06-10 — `892f080f` (combined S49+S55) | ~30min | Flagged by S33 agent: depends on S54. Mirror of S49 for the quote-templates page. **Shared-file hazard:** touches `frontend/src/App.jsx`. **Parallel-safe** with everything not also touching App.jsx (S49 also touches App.jsx — serialise or batch into one S49+S55 combined sweep). **Combined-slice rationale (S49+S55):** both rows touch the same two shared-file hazards (App.jsx + Sidebar.jsx); dispatching as a single sweep avoided the serialise overhead. **Shipped:** App.jsx lazy import `TravelCancellationPolicies = lazy(() => import("./pages/travel/CancellationPolicies"))` adjacent to TravelQuoteTemplates + `<Route path="travel/cancellation-policies" element={<TravelOnly><TravelCancellationPolicies /></TravelOnly>} />` adjacent to the existing travel/quote-templates Route. Sidebar.jsx new `Ban` icon import + `isManager`-gated `<Link to="/travel/cancellation-policies" icon={Ban} label="Cancellation Policies" />` in the Invoices/Milestones billing cluster (under Milestones). Test surface delta: Sidebar.test.jsx +3 cases (ADMIN sees /href + MANAGER sees /href + USER hidden) + CancellationPolicies.test.jsx +1 case (App.jsx route registration pin via static-grep against `../App.jsx` source). |
| **S56** | InvoiceDetail.jsx void-confirmation modal — surface policy preview before confirm | `frontend/src/pages/travel/InvoiceDetail.jsx` (extend void-modal with `GET /invoices/:id/cancel-preview` call — see S58 — and render "Refund: ₹X per TMC Default tier — 14d before service") + tests | ✅ DONE 2026-06-10 — `ae9d5ee5` | ~½d | Flagged by S33 agent: today the void modal just asks for `reason` — operators won't know what they're triggering until after the void. Depends on S33 ✅ + S58 (cancel-preview endpoint). **Parallel-safe** with everything except concurrent InvoiceDetail.jsx edits. **Drift-pin (target surface):** there is no `frontend/src/pages/travel/InvoiceDetail.jsx` — the actual operator-facing void surface lives in `frontend/src/pages/travel/InvoicesAdmin.jsx` (the list page, where edit/PDF/delete buttons already render per row). Shipped: new `Ban` icon button per row (hidden once Voided — terminal state) → opens a dedicated void-confirmation modal that fires `GET /api/travel/invoices/:id/cancel-preview` on open. Preview panel renders ONE of: (a) `reason='OK'` → "Refund: ₹X per &lt;policyName&gt; (Y% — Zd before service)" with INR digit-grouping via `toLocaleString('en-IN')`; (b) `reason='NO_POLICY_RESOLVED'` → "No matching cancellation policy. Voiding will NOT auto-issue a credit note."; (c) fetch failure (network / 404 / 409) → "Could not load refund preview — review the policy before confirming." (Confirm button stays available — preview is informational, not a hard gate). Discriminator is `preview.reason` (NOT `refundAmount === null`) since `refundAmount: 0` is a valid no-refund-but-policy-matched state. Reason textarea required (5..500 chars per backend INVALID_VOID_REASON). Confirm fires POST `/:id/void` with `{reason}` (S33's dedicated audit-logged void endpoint, separate from the existing edit-modal PUT-status transition); success toast contextually names the policy when S33's auto-CR-NOTE issuance fires. **Test surface delta:** +9 cases on `frontend/src/__tests__/InvoicesAdmin.test.jsx` (35 total, was 26): modal opens + preview fetch fires; OK refund line; NO_POLICY_RESOLVED warning line; preview-fetch failure soft error; reason textarea + POST `/:id/void` payload; short-reason validation skips POST; Voided rows hide the button; Cancel closes the modal; auto-issued credit note success toast names the policy. |
| **S57** | Seed defaults — extend `prisma/seed-travel.js` with TMC Default + RFU Default cancellation policies | `backend/prisma/seed-travel.js` (or `seed-wellness.js` — whichever is the canonical Travel seeder) + verify on local box | ✅ DONE 2026-06-10 — `4196594c` | ~½d | Flagged by S33 agent: demo box needs working defaults so operators see the auto-issuance flow without manual config. Depends on S33 ✅. **Parallel-safe** with everything else. **Shipped**: `seedDefaultCancellationPolicies(tenantId)` helper added to `backend/prisma/seed-travel.js` (call-site in `main()` after `seedStarterBrandKits`). Two policies seeded — TMC Default (subBrand=tmc, tiers 60d/100% → 30d/50% → 7d/25% → 0d/0%, "Standard TMC school-trip cancellation policy") + RFU Default (subBrand=rfu, tiers 90d/100% → 45d/75% → 14d/50% → 0d/0%, "Standard RFU Umrah-trip cancellation policy"). Idempotent — `findFirst` on (tenantId, name) → create-or-update branch; update path touches `description` + `isActive` only, preserves operator-tuned `tiersJson` across re-runs. Text-shape vitest pin landed at `backend/test/prisma/seed-travel-cancellation-policies.test.js` (11 cases — function present, call-site wire-in, both policy shapes, tier ladders, idempotency contract, tenant-scoping). Demo-DB seed re-run not executable from this dispatch (network restriction on 163.227.174.141); seeder will run cleanly on next deploy / next operator-invoked `node prisma/seed-travel.js` and is idempotent on demo. |
| **S58** | Public `GET /api/travel/invoices/:id/cancel-preview` endpoint — call `resolveCancellationOutcome()` without voiding | `backend/routes/travel_invoices.js` (extend with new endpoint — ~15 LOC wrapping the existing helper) + tests | ✅ DONE 2026-06-10 — `c634af14` | ~½d | Flagged by S33 agent: surfaces the refund preview to the void modal (S56) so operators can see refund amount BEFORE confirming the void. Helper `resolveCancellationOutcome()` already extracted for re-use. Depends on S33 ✅ + needed by S56. **Parallel-safe** with everything except concurrent travel_invoices.js edits. **Shipped**: `GET /api/travel/invoices/:id/cancel-preview` (verifyToken + verifyRole(ADMIN,MANAGER) + requireTravelTenant + loadParentInvoice for tenant + sub-brand scoping). Read-only — wraps the existing `resolveCancellationOutcome()` helper, no DB writes (zero `update` / `create` / `auditLog.create` calls). Response envelope: `{ invoiceId, status, policyApplied: {policyId, policyName, tier, refundPercent}\|null, refundAmount\|null, refundPercent\|null, daysBeforeServiceStart\|null, serviceStartDate\|null, reason: 'OK'\|'NO_POLICY_RESOLVED' }`. Error codes: 400 INVALID_ID + 403 SUB_BRAND_DENIED (from loadParentInvoice) + 404 INVOICE_NOT_FOUND + 409 ALREADY_VOIDED (resource-state conflict — stricter tier than the void endpoint's 400 because the preview can't ever recover from a voided state). 6 vitest cases extended onto `backend/test/routes/travel-invoice-void.test.js` (happy path, cross-tenant 404, already-voided 409, no-policy-resolved, idempotency-across-2-calls, USER 403); full file 25/25 GREEN. Pairs with S56 (InvoiceDetail.jsx void-modal consumer). |
| **S59** | Replace inline `phiReadGate` const in `routes/wellness.js` with `makePhiReadGate()` factory from middleware | `backend/routes/wellness.js` (~25 callsites — single-file rename: `const phiReadGate = makePhiReadGate()` using the new factory) + extend wellness vitest | ✅ DONE 2026-06-10 — `0e5a88f8` | ~½d | Flagged by S42 agent: S42 shipped `backend/middleware/phiReadGate.js` exporting `makePhiReadGate()` but left the 25-callsite inline `const` in wellness.js untouched (cross-cutting rename was out of scope). Pure mechanical rename, isolated to one file. Depends on S42 ✅. **Parallel-safe** with everything except concurrent wellness.js edits. **Shipped**: inline `const phiReadGate = verifyWellnessRole([...], { anyOfPermissions: [...], deny: ["helper"] })` (lines 365-389 pre-change) replaced with `const phiReadGate = makePhiReadGate()` imported from `backend/middleware/phiReadGate.js`. All 37 callsites of `phiReadGate` below in wellness.js continue to work unchanged (same local binding, byte-equivalent middleware semantics). 73/73 wellnessRole + wellnessOwnership vitest cases stay GREEN; 0 lint errors; node --check passes. No extra vitest needed — the middleware module's contract is already covered by S42's surface and the existing wellnessRole.test.js (which underlies makePhiReadGate). |
| **S60** | 🔵 BLOCKED — Patient.name slim-shape privacy review | `backend/lib/listProjection.js` (potential masked-by-default flip on slim path) + downstream consumer audit | 🔵 BLOCKED | ~½d (impl) + product call | Flagged by S42 agent: S42's Patient slim projection includes `name` (so picker UI can disambiguate rows) with `maskRows()` applied for low-trust viewers. **Blocked on:** product call deciding whether even masked `name` is too-PII for default disclosure on summary path. **Unblocked when:** Rishu (Enhanced Wellness owner) clarifies HIPAA posture on the slim-shape disclosure boundary. |
| **S61** | Hard-pin audit-shape contract in e2e spec for PHI list reads | `e2e/tests/wellness-clinical-api.spec.js` (extend with strict-pin audit-shape contract — fetch `/api/audit` deterministically by run-tag rather than soft fall-through) + `e2e/tests/audit-coverage-api.spec.js` (extend with PHI list-read audit-coverage) | ✅ DONE 2026-06-10 — `be3a614f` | ~½d | Flagged by S42 agent: the S42 e2e test pins the `shape` audit field SOFTLY (falls through gracefully if recent row can't be located). Stricter pin requires a dedicated audit-shape contract slice. Depends on S42 ✅. **Parallel-safe** with everything else. |
| **S62** | Schema additions for wellness Patient/Prescription columns missing per PRD slim-shape spec | `backend/prisma/schema.prisma` (additive nullable: `Patient.firstName`, `Patient.lastName`, `Patient.displayName`, `Patient.lastVisitDate` denormalized cache; `Prescription.status`, `Prescription.dispensedAt`) + migration safety | ⬜ TODO | ~1d | Flagged by S42 agent: PRD slim-shape spec called for these fields but they don't exist on the schema. S42 pinned to actually-existing columns; this slice adds the missing ones. Additive nullable — no bless marker. Once landed, S42's slim projections should be updated to include the new columns (paired follow-up). Depends on S42 ✅. **Parallel-safe** with everything except concurrent schema.prisma slices (serialise with S33/S37/S47 if queued). |
| **S63** | Frontend "Suggest itinerary" button on Itineraries.jsx | `frontend/src/pages/travel/Itineraries.jsx` (add CTA button + modal for destination/durationDays/budgetTier/themeJson + render returned suggestionJson) + `frontend/src/__tests__/Itineraries.test.jsx` (extend with new test cases) | ⬜ TODO | ~1d | Flagged by S46 agent: PRD FR-3.6 step (a) calls for a "Suggest itinerary" CTA. Wires `POST /api/travel/itineraries/suggest` (shipped in S46) into a modal where the operator picks destination/duration/budget/theme, then renders the `suggestionJson` so the operator can review + edit before materialising items. Depends on S46 ✅. **Parallel-safe** with everything except concurrent Itineraries.jsx edits. |
| **S64** | Visual editor (S9) blank-state integration with suggest endpoint | `frontend/src/pages/travel/ItineraryDayEditor.jsx` (blank-state trigger calling `POST /itineraries/suggest`) + tests | ⬜ TODO | ~½d | Flagged by S46 agent: PRD FR-3.6 step (a) also lists the S9 visual editor's blank-state as a second trigger surface for the suggest endpoint. Same call wiring as S63's button; renders into the editor instead of a modal. **Depends on S9 (NOT yet shipped) + S46 ✅** — dispatch only after S9 lands. |
| **S65** | Extend S51 logo-embed pattern to 4 sibling travel PDF renderers | `backend/services/pdfRenderer.js` (call `fetchLogoBuffer` + `doc.image()` from `renderTravelItineraryPdf` / `renderTravelQuotePdf` / `renderTravelDiagnosticPdf` / `renderTmcReadinessReport` / `renderTravelStallPersonalisedPdf` — same shape as S51's `renderTravelInvoicePdf` extension) + extend `backend/test/services/travel-sibling-pdfs-brand-kit.test.js` with image-embed pins | ✅ DONE 2026-06-10 — `6e71eb71` | ~½d | Flagged by S52 agent: S52 plumbed `branding.thumbnailUrl` end-to-end through the resolver for all 5 sibling renderers but stopped short of embedding. Today only `renderTravelInvoicePdf` (S51) embeds; the other 5 still render text-placeholder logos (e.g. `renderTravelQuotePdf` calls `(q.brandKit && q.brandKit.logoUrl)` to print a `[Logo: <url>]` placeholder). Sweep extends the S51 `fetchLogoBuffer` + `doc.image()` pattern to the 4 sibling renderers + removes the text-placeholder in the quote PDF. Depends on S51 ✅ + S52 ✅. **Parallel-safe** with everything except concurrent `pdfRenderer.js` edits. |
| **S66** | Wire `/embed/*` mount to read per-tenant `embedAllowlistJson` via `readTenantEmbedAllowlist()` | `backend/server.js` (replace `allowIframeEmbedding({ allowList: ["*"] })` with a request-time middleware that calls `readTenantEmbedAllowlist(prisma, req.user?.tenantId)` and falls back to wildcard on null) + `e2e/tests/public-booking-api.spec.js` (extend with per-tenant pin) | ⬜ TODO | ~30min | Flagged by S39 agent: S38 (commit `7a229c92`) shipped the embed mount with hardcoded `allowList: ['*']` because the column didn't exist. S39 (commit `56832537`) added the column + wired `readTenantEmbedAllowlist()` to a real read. S66 now connects them: the embed mount reads the tenant's allowlist at request time. Falls back to `['*']` when the tenant has no allowlist set (back-compat). Depends on S38 ✅ + S39 ✅. **Shared-file hazard:** touches `backend/server.js`. **Parallel-safe** with everything not also touching server.js. |
| **S67** | Per-tenant SupplierCredential resolver for `adsGptClient` | `backend/services/adsGptClient.js` (extend with `getAdsGptKey(tenantId)` mirroring S45 `getLlmKey` shape; replace direct `process.env.ADSGPT_API_KEY` reads) + `backend/test/services/adsGptClient.test.js` (extend) | ✅ DONE 2026-06-10 — `996bb4f2` | ~½d | Flagged by S45 agent: S45 shipped `getLlmKey()` for LLM clients but left adsGptClient on direct ENV reads. Mirror the same SupplierCredential → ENV fallback pattern with category `'adsgpt-key'`. Note: `process.env.ADSGPT_API_KEY` doesn't exist yet — slice can ship the lookup layer with a placeholder until the actual env var lands. Depends on S45 ✅. **Parallel-safe** with everything else. |
| **S68** | Per-tenant SupplierCredential resolver for `ratehawkClient` with multi-field shape | `backend/services/ratehawkClient.js` (extend with `getRatehawkCreds(tenantId)` returning `{ keyId, apiKey }`; SupplierCredential row uses both `loginIdEncrypted` (key-id) AND `passwordEncrypted` (api-key)) + tests | ✅ DONE 2026-06-10 — `7749b84d` | ~½d | Flagged by S45 agent: RateHawk uses key-id + api-key — S45's single-string `getLlmKey()` shape isn't enough. Needs a new helper returning a multi-field object. Category: `'ratehawk-cred'`. Depends on S45 ✅. **Parallel-safe** with everything else. |
| **S69** | Per-tenant SupplierCredential resolver for `callifiedClient` | `backend/services/callifiedClient.js` (extend with `getCallifiedKey(tenantId)` mirroring S45 `getLlmKey` shape) + tests | ✅ DONE 2026-06-10 — `ead96563` | ~½d | Flagged by S45 agent: callifiedClient (AI calling) still uses direct ENV. Category: `'ai-calling-key'`. Same pattern as S67. Depends on S45 ✅. **Parallel-safe** with everything else. Note: callifiedClient was already a STUB with no ENV API-key read; S69 added `getCallifiedKey(tenantId)` as the swap-point ahead of cred-drop (placeholder env-var `CALLIFIED_API_KEY` per docs/CALLIFIED_INTEGRATION_SETUP.md §5) + wired both `initiateCall` and `fetchCallResult` to resolve via `module.exports.getCallifiedKey` (CJS self-mocking seam). Tests: 18 → 30 (+12). |
| **S70** | Per-tenant SupplierCredential resolver for `tmcDiagnosticPrompts` | `backend/services/tmcDiagnosticPrompts.js` (extend with per-tenant Gemini key lookup via `llmRouter.getLlmKey(tenantId, 'gemini-flash')`) + tests | ✅ DONE 2026-06-10 — `8b82c107` NO-OP (phantom slice) | ~½d | **PHANTOM SLICE — no work needed.** Verified by S70 dispatcher: (1) `tmcDiagnosticPrompts.js` is a PURE prompt-building module (header line 3 documents: "NO database. NO LLM call. NO fetch. NO file I/O.") — it does NOT read any API key. (2) The two tasks it emits (`tmc-readiness-narrative` + `tmc-sales-brief`) are NOT in `TASK_ROUTING` in `lib/llmRouter.js` → they hit the unknown-task catch-all in `pickModel()` (line 198-201) which returns `claude-opus-4-7` (Claude reasoning), **NOT `gemini-flash`** as the slice text claims. (3) Per-tenant key resolution is already handled by `llmRouter.routeRequest()` (which `routes/travel_diagnostics.js` calls at lines 2348 + 2513 with `tenantId: diag.tenantId`) — when real-mode lands, that's where the `getLlmKey()` call belongs, not in the pure prompt-builder. (4) The S45 close-out (line 85) already explicitly flagged this: "**Out of scope (follow-ups):** ... `tmcDiagnosticPrompts.js` is a pure prompt-builder (no env check, no work)." — S70 was filed despite that warning. Marker flipped to ✅ DONE with NO-OP rationale; no code change required. |
| **S71** | 🔵 BLOCKED — Gemini real-mode wire-in for `marketingFlyerCopyLLM` | `backend/services/marketingFlyerCopyLLM.js` (swap stub `callGemini()` throw for `@google/generative-ai` dispatch — TODO block + dispatch sketch already laid out in S15) + tests | 🔵 BLOCKED | ~½d | Flagged by S15 agent: S15 shipped the stub-mode module with `realModeEnabled()` + try/catch + envelope shape + log line ALL pre-wired. The actual `callGemini` body is a throw-stub awaiting the cred drop. **Blocked on:** Q-AI-3 / Q11 (`GEMINI_API_KEY` from Yasin — same cred unlock as S44 for itinerarySuggestLLM; both swap on the same drop). **Unblocked when:** Yasin provisions the Gemini key. Until then, every flyer-copy call returns the canned stub. Depends on S15 ✅. **Parallel-safe** with S44 (different swap file; sibling-slice). |
| **S72** | 🔵 BLOCKED — DALL-E / Stability real-mode wire-in for `marketingFlyerImageLLM` + per-tenant SupplierCredential lookup | `backend/services/marketingFlyerImageLLM.js` (swap `callImageProvider()` throw for OpenAI `images.generate` DALL-E path / Stability v2beta REST + base64-to-S3 fallback; replace ENV-only `resolveProvider()` with per-tenant SupplierCredential category `'image-key'` lookup) + tests | 🔵 BLOCKED | ~1d | Flagged by S16 agent: S16 shipped the stub-mode module with provider-selection (DALL-E primary / Stability fallback) + envelope shape ALL pre-wired. The actual `callImageProvider` body is a throw-stub; `resolveProvider()` is ENV-only today. **Blocked on:** Q-MF-2 (`OPENAI_API_KEY` OR `STABILITY_API_KEY` from Yasin). **Unblocked when:** Yasin provisions an image-gen API key. Until then, every flyer-image call returns the canned-URL stub. Combines real-mode swap + per-tenant cred lookup (mirrors S67/S68/S69 pattern but for image creds). Depends on S16 ✅. **Parallel-safe** with S71. |
| **S73** | Split image-LLM into separate `'image-llm'` cap envelope | `backend/services/marketingFlyerImageLLM.js` (use distinct INTEGRATION='image-llm' for `checkBudgetCap` + `computeMonthlySpendCents`) + `backend/lib/tenantSettings.js` (add `IMAGE_LLM_MONTHLY_CAP_USD_CENTS` default) + tests | ⬜ TODO | ~½d | Flagged by S16 agent: S16's image-gen currently shares the LLM monthly envelope (INTEGRATION='llm') with text tasks. DALL-E 3 HD ($0.12/image) can eat the text-LLM budget on a runaway burst. Separating into its own cap envelope lets ops set finer per-tenant control. Depends on S16 ✅. **Parallel-safe** with S72 (different concerns; both can ship concurrently). |
| **S74** | Install `puppeteer` npm dep + wire real PNG render in `flyerRenderEngine` | `backend/package.json` (add `puppeteer` dep) + `backend/services/flyerRenderEngine.js` (swap `stub-1x1` PNG branch for real headless-Chrome screenshot) + tests | ⬜ TODO | ~½d | Flagged by S17 agent: S17 shipped the engine surface with deterministic 1×1 stub PNGs (header `X-Flyer-Render-Engine: stub-1x1`). Puppeteer install was outside slice scope. Once dep lands, PNG branch flips to real screenshots with zero caller-side change. Depends on S17 ✅. **Parallel-safe** with everything else; `npm audit` gate may surface CVEs requiring allowlist entry. |
| **S75** | Extend `lib/flyerPdfRender.PAPER_SIZES` to include A5 | `backend/lib/flyerPdfRender.js` (add `a5: 'A5'` to PAPER_SIZES table) + tests | ⬜ TODO | ~10min | Flagged by S17 agent: `lib/flyerPdfRender.js`'s `PAPER_SIZES` table is `{a4, us_letter}` only; S17's engine passes `aspect: 'a4'` for both `pdf-a4` AND `pdf-a5` formats today (so A5 currently renders at A4 size). One-line lib edit + pin via vitest. Depends on S17 ✅. **Parallel-safe** with everything else. |
| **S76** | Wire `e2e/tests/travel-flyer-templates-api.spec.js` into deploy.yml + coverage.yml gate-spec lists | `.github/workflows/deploy.yml` + `.github/workflows/coverage.yml` (add spec to gate lists) | ⬜ TODO | ~5min | Flagged by S17 agent: S17 shipped 14 new Playwright cases in `travel-flyer-templates-api.spec.js` but the spec isn't in either workflow's gate list. Per the standing rule for new routes. Tiny 2-file ops fix per the `wiring-spec-into-gate` skill. Depends on S17 ✅. **Sequential-only** (deploy.yml is concurrent-write-prone). |
| **S77** | Frontend `Download` dropdown on FlyerStudio page calling POST `/:id/render` per format | `frontend/src/pages/travel/FlyerStudio.jsx` (or sibling — verify with grep) (add Download dropdown menu: PDF A4 / PDF A5 / Square PNG / Portrait IG / Landscape FB) + RTL tests | ⬜ TODO | ~½d | Flagged by S17 agent: S17 shipped the backend endpoint but no frontend caller. FlyerStudio needs a Download dropdown that fires `POST /:id/render` with the chosen format + downloads the returned buffer as a file. Depends on S17 ✅. **Parallel-safe** with everything except concurrent FlyerStudio.jsx edits. |

## Dispatch DAG

```
S1 / S2 / S3 / S4 / S5 ── parallel — all codeable today (Travel Security)
S8 ──> S9 + S10 ──> S11 ──> S12 (Itinerary editor chain)
S13 / S14 — parallel (Itinerary)
S15 / S16 / S17 ── parallel — S18 + S19 depend on S17 (Marketing Flyer)
S30 / S31 / S32 / S33 / S34 — parallel (per-PRD residuals)
```

**Codeable-now count:** 22 rows (S1-S5, S8-S19 (less the BLOCKED ones), S30-S34). Estimated total: ~50-65 engineering days. With 3 parallel agents per tick, ~10-15 wall-clock days.

**🔵 BLOCKED count:** 11 rows (S6, S7, S20, S21, S22, S23-S29). Awaiting product calls or creds.

## Standing rules

- NO `Co-Authored-By: Claude` trailer.
- `git pull --ff-only origin main` BEFORE editing.
- `git fetch && git pull --rebase && git commit --only <files>` per parallel-wave standing rule.
- HEREDOC `.tmp-agent-sNN-msg.txt` in project root (NOT `/tmp/`).
- Each agent flips their row marker ⬜ → ✅ DONE 2026-06-NN in the same commit (or via SHA-backfill follow-up per the established pattern).
- Schema migration safety: additive nullable doesn't need bless marker; UNIQUE / NOT NULL / column-drop / type-narrow does.
- If a dispatched agent discovers their slice has a NEW blocker not listed here, they MUST flip the marker to 🔵 BLOCKED with `Blocked on: <reason>` rather than power through with a stub.
