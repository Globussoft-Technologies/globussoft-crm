> Archived from TODOS.md on 2026-05-17 — this was the active session handoff at the time it was written. See [docs/handoffs-archive/README.md](README.md) for the archive convention.

## 🏁 SESSION HANDOFF (2026-05-09 — v3.5.0 release: 4 greenfield feature areas + 6-round deploy-gate stabilization)

**HEAD on origin/main:** `0dd1f84` (Wave 5 Agent XX — vitest extension on 3 lowest-coverage cron engines: leadSla / lowStock / scheduledEmail, +76 cases). Round 6b sibling commit `68180bc` (Agent UU's preventive visit-conflict sweep) sits one commit back; the version-bump commit (this one) sits on top. Per-push gate state is **in_progress on `68180bc` and pending on `0dd1f84`** at write-time; the version bump should land on the first green of the post-6b chain. Frontend RTL component tests for the 4 new feature pages remain the v3.5.1 carry-over.

### Why this session

Multi-wave parallel session: 13 agents across Wave 1 (4 small fixes + #644 PR-merge follow-up + PRD verification), Wave 2 (4 greenfield feature areas — POS / Attendance+Leave / WhatsApp 2-way / Booking widget), Wave 3 (audit + coverage extension), Wave 4 (gdpr.spec.js timing flake fix), and Wave 5 (this version-bump). The greenfield wave came directly out of the 2026-05-08 Google Doc audit's "Confirmed-missing entirely" rows. The 6-round deploy-gate triage cleared a 25-hour outage that had been masking every Wave 1/2/3 commit behind a chronic-red gate.

### What v3.5.0 delivered (1 paragraph)

Four entirely new product surfaces (POS backbone with Register / Shift / Sale / polymorphic line items; Attendance + Biometric webhook + Leave Management; WhatsApp 2-way completion with Threads + agent assignment + opt-out; Booking widget completion with `bookingType` enum + at-home address + UTM capture). Plus a Wave-3 coverage extension (`eventBus.js` 38% → 83% lines, +80 vitest cases across 3 modules), an orchestrator depth audit (PRD §6.7 verdict: deep, +13 vitest pins), an e2e brittleness audit (correctly identified the stale "41 failures" framing as 1 actually-open item), the gdpr.spec.js export-timing fix, the Estimate `validUntil` upper-bound cap, the `/send-now` 502 → 200+success:false flip for Cloudflare proxy compatibility, the #632 follow-up Surveys+Loyalty aria-label sweep, the PRD §14.4 demo-script + curl-wrapper, and the 6-round deploy-gate triage that cleared a 25-hour outage. Issue #457 gained 10 new manual-QA-only sections (8–17) for hardware / device / cross-app fidelity surfaces that genuinely cannot be automated.

### What still needs to happen (carry-over for v3.5.1)

- **Frontend RTL component tests for the 4 new feature pages.** PointOfSale.jsx / Attendance.jsx / Leave.jsx / WhatsAppThreads.jsx shipped without RTL component tests because the Wave-2 agents prioritised the API gate spec. Frontend vitest suite stays at 6 files / ~35 tests until those land. ~6h once test scaffolding is decided per page.
- **B-03 SendGrid Sender Identity** — still operator-blocked. Path A (Single-Sender Verification, ~2 min) or path B (Domain Authentication via DNS, ~10 min). Until B-03 ships, no email delivers from demo regardless of code; `/send-now` will surface `SENDGRID_REJECTED` (now 200+success:false per `d194492`) but the underlying SendGrid call still fails.
- **Leave carry-forward + encashment cron** — `LeavePolicy` columns are present; the nightly job is not yet implemented.
- **Booking widget pincode-distance-based travel time** — currently a flat 30-minute default for IN_HOME bookings.
- **WhatsApp opt-out re-opt-in audit-row policy product call** — current `97b157f` requires admin to re-opt-in but doesn't require explicit user consent (DPDP §11). Worth Rishu's input.

### Three things to do first next session

1. **Confirm v3.5.0's deploy gate goes green and tag.** Verify the version-bump commit lands green via `gh run list --workflow=deploy.yml --limit 5`. If green, push tag `v3.5.0` to fire `e2e-full.yml` release-validation against demo. If red on a new failure mode, drop everything and run `.claude/skills/triaging-stuck-deploy-gate/SKILL.md` — today's 6-round chase showed how fast cascades can stack.
2. **Add frontend RTL component tests for the 4 new feature pages.** PointOfSale.jsx + Attendance.jsx + Leave.jsx + WhatsAppThreads.jsx all need component tests at `frontend/src/__tests__/`. Pattern to copy: any of the 14 existing files (CommandPalette / DealModal / OwnerDashboard etc.). Mock `fetchApi` per file. ~6h sequential, parallel-safe across the 4 files.
3. **Close B-03 — SendGrid Sender Identity.** This has been blocking real email delivery from demo since the v3.4.13 SendGrid swap on 2026-05-06. Two paths: (a) tell me which address you verified in SendGrid + I'll SSH-update demo's `backend/.env` with `SENDGRID_FROM_EMAIL=<verified>`; (b) set up Domain Authentication for `crm.globusdemos.com` in SendGrid + add the CNAME records. The reusable [`scripts/apply-turnstile-env.py`](scripts/apply-turnstile-env.py) pattern handles backup-and-rollback safely.

### Per-push gate state (post v3.5.0, at HEAD `0dd1f84`)

~82 specs / ~2,651 API tests + 49 vitest files / ~1,365 unit tests + 6 frontend vitest / ~35 tests = **~4,051 tests on every push** (counts include Agent XX's three new cron-engine vitests, +76 cases).

### Long tail still open

- **#457** Manual-only QA umbrella — intentional, stays open. Sections 8–17 added today (codebase-scan: hardware, MFA/SSO, partner-API live integration, file-cross-app fidelity, embedded-widget cross-origin, POS hardware, WhatsApp device rendering, booking geocoding, biometric devices, calendar-sync round-trip, photo-tab device fidelity).
- **#523** responsive.css 11 brittle attribute selectors → class-based — ⬜ open, ~2-3h.
- **#431** Privacy retention silent-revert — ⬜ open, awaiting fresh repro.
- **#527 product-policy call** — Rishu input needed on telecaller-can-read-all + professional-can-edit-any policies.
- **#555 / #558 / #564 / #565** — design / product calls (see "TASKS NEEDING USER ATTENTION" block below).
- **PRD 14.4 Callified webhook stand-in** — demo-script shipped; partner team's auto-post webhook still in flight.
- **AdsGPT silent SSO provisioning + back-link** — external-team deliverable per PRD §14.3 verification.

Earlier session arc (2026-05-08): PR #644 merged + Google Doc audit identified the 4 confirmed-missing-entirely surfaces (POS + Attendance/Leave + WhatsApp Threads + Booking-widget completion) that became today's Wave 2 greenfield work. Earlier than that: see CHANGELOG entries for v3.4.14 (2026-05-06 pen-test sweep) and v3.4.13 (2026-05-06 24-issue closure arc).

---

