{
  "name": "globussoft-wa-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone WhatsApp Gateway — isolates whatsapp-web.js + Puppeteer/Chromium from the CRM backend. Transport only (sessions, QR, send, inbound/ack event stream). Owns NO CRM database. See docs/WHATSAPP_GATEWAY_EXTRACTION.md.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "qrcode": "^1.5.4",
    "whatsapp-web.js": "^1.34.7",
    "puppeteer": "^25.1.0"
  }
}
